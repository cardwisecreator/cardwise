{
  "name": "perklah-sg",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "postinstall": "pnpm --filter @perklah/db prisma generate",
    "prebuild": "pnpm db:push && pnpm db:seed",
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "pnpm --filter @perklah/db prisma db push",
    "db:seed": "pnpm --filter @perklah/db prisma db seed"
  },
  "devDependencies": { "turbo": "^2.3.3", "typescript": "^5.7.2" }
}
