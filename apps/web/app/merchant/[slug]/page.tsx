import {db}from"@/lib/db";import{notFound}from"next/navigation";export const dynamic="force-dynamic";
export default async function Merchant({params}:{params:Promise<{slug:string}>}){const offer=await db.offer.findUnique({where:{slug:(await params).slug}});if(!offer||offer.status!=="PUBLISHED")notFound();return <main className="wrap section"><div className="eyebrow">Merchant reward</div><h1>{offer.merchant}</h1><article className="card"><h3>{offer.title}</h3><p>{offer.description}</p><div className="stats">{offer.minSpend&&<span className="stat">Min. spend S${offer.minSpend}</span>}<span className="stat">Ends {offer.validTo.toLocaleDateString("en-SG")}</span></div><a className="button green" href={offer.sourceUrl} target="_blank">Official offer terms ↗</a></article></main>}
