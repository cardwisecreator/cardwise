import { createHash } from "crypto"; import { NextRequest, NextResponse } from "next/server"; import { z } from "zod"; import { db } from "@/lib/db"; import { sessionToken } from "@/lib/session";
const lead=z.object({name:z.string().min(2).max(80),email:z.string().email(),phone:z.string().regex(/^\+?[0-9 ()-]{8,20}$/),password:z.string().min(8),marketingConsent:z.boolean().default(false)});
export async function POST(r:NextRequest){const v=lead.safeParse(await r.json());if(!v.success)return NextResponse.json({error:"Use a valid name, email, Singapore phone number and an 8+ character password."},{status:400});const passwordHash=createHash("sha256").update(v.data.password).digest("hex");await db.lead.upsert({where:{email:v.data.email},update:{name:v.data.name,phone:v.data.phone,passwordHash,marketingConsent:v.data.marketingConsent},create:{...v.data,passwordHash}});const response=NextResponse.json({ok:true},{status:201});response.cookies.set("perklah_session",sessionToken(v.data.email),{httpOnly:true,secure:process.env.NODE_ENV==="production",sameSite:"lax",maxAge:60*60*24*30,path:"/"});return response;}
